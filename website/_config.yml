---
collections:
    development_manual:
        output: true
        layout: default
    installation:
        output: true
        layout: default

defaults:
    - scope:
        path: ""
        type: installation
      values:
        layout: default
    - scope:
        path: "index.md"
      values:
        layout: default
---  
